apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-job.fullname" . }}
  labels:
    {{- include "helm-job.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-job.selectorLabels" . | nindent 4 }}
